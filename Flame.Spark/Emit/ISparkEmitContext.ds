using System;
using System.Collections.Generic;

namespace Flame.Spark.Emit
{
	public interface ISparkEmitContext
	{
		void Emit(OpCode Value);
		void Emit(OpCode Value, bit8 Data);
		void Emit(OpCode Value, bit16 Data);
		void Emit(OpCode Value, bit32 Data);
		void Emit(OpCode Value, bit64 Data);
	}
}