language: csharp
install:
  # Restore nuget packages
  - make nuget

script:
  # Build Flame using csc
  - msbuild /p:Configuration=Release /verbosity:quiet /nologo Flame.sln

  # Build Flame using ecsc
  - make

  # Run the tests
  - make test
