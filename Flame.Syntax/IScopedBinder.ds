using System;
using System.Collections.Generic;
using System.Text;
using Flame.Binding;

namespace Flame.Syntax
{
	public interface IScopedBinder : INamespacedBinder
	{
		IScopedBinder PushScope(string Name);
		IScopedBinder PushScope(string Name, INamespace CurrentNamespace);
	}
}