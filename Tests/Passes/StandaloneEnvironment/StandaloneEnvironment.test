// A compare-test test description.

build
{
    $(dsc) $(Filename).ds -platform ir -S -environment=standalone -o bin/$(Filename).fir $(Flags);
}

configs
{
    Og { Flags = -Og; }
    O2 { Flags = -O2; }
    O3 { Flags = -O3; }
}

init
{
    Filename = StandaloneEnvironment;
}
