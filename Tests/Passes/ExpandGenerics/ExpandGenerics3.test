// A compare-test test description.

build
{
    $(dsc) $(Filename).ds -platform clr -o bin/$(Filename).exe $(Flags);
}

run
{
    return = @($(net-runtime) bin/$(Filename).exe);
}

configs
{
    Og { Flags = -Og -fexpand-generics; }
    O2 { Flags = -O2 -fexpand-generics; }
    O3 { Flags = -O3 -fexpand-generics; }
}

init
{
    Filename = ExpandGenerics3;
    return = @($echo 0)@($echo 0);
}
