public class BitcastTest
{
    public const this();

    public bit16 Test(bit32 Value)
    {
        return (bit16)Value;
    }

    public bool StupidIs(BitcastTest Instance)
    {
        return Instance is BitcastTest;
    }
}