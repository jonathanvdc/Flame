using System;
using System.Collections.Generic;
using System.Text;

namespace Flame.Build
{
    public virtual class DescribedField : DescribedTypeMember, IField
    {
        public const this();
        public const this(set string Name);
        public const this(set string Name, set IType FieldType);
        public const this(set string Name, set IType FieldType, set bool IsStatic);

        public IType FieldType { const get; set; }
    }
}