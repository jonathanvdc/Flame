using System;
using System.Collections.Generic;

public interface IGenericTest<T>
{
    T GetTester();
}

public static class TestImpl : IGenericTest<ITest>, ITest
{
    public int Value { const get return 5; }
    public ITest GetTester() return this;

    public const T Identity<T>(T Value)
    {
        return Value;
    }

    public const ITest GetStringValue()
    {
        return Identity<ITest>(this);
    }
}