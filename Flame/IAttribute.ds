using System;

namespace Flame
{
    public interface IAttribute
    {
        /// <summary>
        /// Gets the attribute's type.
        /// </summary>
        IType AttributeType { const get; }

        /// <summary>
        /// Gets the attribute's value.
        /// </summary>
        IBoundObject Value { get; }
    }
}
